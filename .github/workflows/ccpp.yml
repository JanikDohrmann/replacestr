name: C/C++ CI

on: [push]

jobs:
  build-windows:
    name: Windows build

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build (MSVC)
      run: |
        cmake --build .
